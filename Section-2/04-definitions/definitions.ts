/**
 * Hover greet
 * Hover greet with Crtl
 * 
 * F12 -> go to definition || Crtl + Click
 * Shift + F12 -> look references
 * Ctrl + Shift + F12 -> look definition || Alt + F12 Mac
 * 
 * Open new file + Crtl + W to close (same as google chrome)
 */

import { greet } from "./functions";

const greeting = greet('Iron man');

console.log(greeting);